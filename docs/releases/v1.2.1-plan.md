# v1.2.1 发布计划

## 状态
- 版本号：v1.2.1
- 计划状态：已发布
- 预计版本类型：patch

## 目标
- [x] 缩短正式发布体感耗时，减少本地和 GitHub Actions 重复打包。
- [x] 增加发布预检脚本，把版本号、文档冻结、release notes、测试和 diff 检查收束成一个命令。
- [x] 为 GitHub 正式发布和 preview artifact workflow 增加 pip cache。
- [x] 正式发布 workflow 复用既有 OpenCV runtime Release 资产，只有 runtime 版本变化或缺少匹配资产时才重建。
- [x] 升级 GitHub Actions checkout/setup/upload-artifact action 主版本，消除旧 Node.js runtime 弃用提示。
- [x] 更新 release-governance skill，沉淀“单一权威打包路径”和稳定附属资产复用原则。

## 范围
本次做：
- 新增 `scripts/prepare_release.ps1`，用于正式发布前预检；默认不生成正式 ZIP，由 GitHub tag workflow 生成正式资产。
- 新增 `docs/release-process.md`，记录快速发布路径、本地正式 ZIP 何时需要、v1.2.0 耗时拆解和后续可选优化。
- 更新 `.github/workflows/publish-release.yml` 和 `.github/workflows/build-preview.yml`，为 Python 依赖安装启用 pip cache。
- 更新 `.github/workflows/publish-release.yml`，从既有 GitHub Release 复用匹配版本的 OpenCV runtime asset；找不到时回退到重新构建。
- 升级 `.github/workflows/publish-release.yml` 和 `.github/workflows/build-preview.yml` 中的 GitHub Actions 主版本。
- 更新 `CHANGELOG.md` 的 v1.2.1 发布记录。

本次不做：
- 不改变业务功能、数据结构或 ZIP 安装根目录。
- 不主动同步或部署 Linux 服务器。

## 版本号判断
- 本次是发布流程和 CI 优化，不改变用户数据结构或核心业务功能，按 patch 发布。

---

## 完成记录

### 重要改动
- 新增发布预检脚本和发布流程文档。
- GitHub 正式发布和 preview artifact workflow 增加 pip cache。
- GitHub 正式发布 workflow 优先复用已有 OpenCV runtime asset；action 版本升级到当前可用新主版本。
- GitHub 正式发布 workflow 改用 PowerShell 显式参数表调用打包脚本，避免 `ReleaseDate` 参数在云端被误判为预览流水号。
- release-governance skill 增加快速发布路径和稳定附属资产复用说明。

### 验证记录
- [x] 发布预检脚本语法检查：PowerShell `scriptblock` 解析通过。
- [x] v1.2.1 GitHub Release workflow：run `28141654008` 成功，总耗时 `182s`；对比 run `28121701414`，总耗时差值 `-84s`；URL https://github.com/winloud/Reimbursement-Tool/actions/runs/28141654008。
- [x] v1.2.1 GitHub Release 资产校验：主包 `reimbursement-tool-v1.2.1-20260625.zip`（约 45.5 MB），OpenCV runtime `opencv-wechat-runtime-opencv-4.10.0.84-win_amd64.zip`（约 60.75 MB）；Release URL https://github.com/winloud/Reimbursement-Tool/releases/tag/v1.2.1。
- [x] v1.2.1 正式 ZIP 内容校验：发布后本地执行 metadata-only 校验，未下载远端主 ZIP；ZIP 内容结构由 GitHub Actions 的 `Validate local release ZIP` 步骤在上传前对本地构建产物完成校验。
- [x] v1.2.1 发布耗时采集：`scripts\collect_release_metrics.ps1 -RunId 28141654008` 已生成 JSON/Markdown 摘要。


- [x] 发布预检脚本完整验证：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\prepare_release.ps1 -Version 1.2.0 -ReleaseDate 20260625` 成功；后端 `210 passed`、前端 `48 passed`，并校验 v1.2.0 版本元数据、CHANGELOG、README、冻结计划、release notes 抽取和 `git diff --check`。
- [x] workflow YAML 解析检查：Python `yaml.safe_load` 可解析 `.github/workflows/publish-release.yml` 和 `.github/workflows/build-preview.yml`。
- [x] OpenCV runtime 复用路径验证：本地模拟正式发布 workflow 的恢复逻辑，可从 GitHub Release `v1.2.0` 下载并识别 `opencv-wechat-runtime-opencv-4.10.0.84-win_amd64.zip`，大小约 60.75 MB。
- [x] 发布预检脚本轻量复验：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\prepare_release.ps1 -Version 1.2.0 -ReleaseDate 20260625 -SkipTests` 成功。
- [x] diff 检查：`git diff --check` 通过；仅有既有 CRLF 转换提示。
- [x] v1.2.1 正式发布预检：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\prepare_release.ps1 -Version 1.2.1 -ReleaseDate 20260625` 成功；后端 `210 passed`、前端 `48 passed`，并校验 v1.2.1 版本元数据、CHANGELOG、README、冻结计划、release notes 抽取和 `git diff --check`。
- [x] v1.2.1 首次 GitHub Release workflow run `28121354721` 失败定位：OpenCV runtime 已成功复用，但 `Build release ZIPs` 步骤中参数数组调用导致 `ReleaseDate` 被误判为 `PreviewSerial`；已改为 PowerShell 显式参数表调用。
- [x] workflow YAML 复验：Python `yaml.safe_load` 可解析 `.github/workflows/publish-release.yml` 和 `.github/workflows/build-preview.yml`。
- [x] workflow 参数修复后正式发布预检：`powershell -NoProfile -ExecutionPolicy Bypass -File scripts\prepare_release.ps1 -Version 1.2.1 -ReleaseDate 20260625` 成功；后端 `210 passed`、前端 `48 passed`，`git diff --check` 通过。
- [x] v1.2.1 GitHub Release workflow：首次 run `28121354721` 失败后修复参数传递并移动 tag；第二次 run `28121701414` 成功，job 用时 `4m21s`，Release URL `https://github.com/winloud/Reimbursement-Tool/releases/tag/v1.2.1`。
- [x] v1.2.1 GitHub Release 资产校验：资产包含 `reimbursement-tool-v1.2.1-20260625.zip`（约 47.7 MB）和复用的 `opencv-wechat-runtime-opencv-4.10.0.84-win_amd64.zip`（约 63.7 MB）；Release 非 draft、非 prerelease。
- [x] v1.2.1 正式 ZIP 内容校验：下载 GitHub Release 主 ZIP 后检查，包含 `README.md`、`zip-upgrade-guide.md`、`upgrade_zip_release.ps1`、根目录 launcher、`current-version.json`、根目录和版本目录 `portable-release.json`、`versions/1.2.1/报销管理.exe`；`current_version` 与 `app_version` 均为 `1.2.1`，数据结构兼容范围 `1-1`，未包含 `data/`、`uploads/`、`logs/`、`browser-profile/`、`vendor/`、`window-state.json`、`release/` 或测试样本。
- [x] v1.2.1 发布耗时对比：v1.2.1 run 总耗时 `266s`，v1.2.0 run 总耗时 `200s`，本次没有加速。OpenCV runtime 成功从 v1.2.0 Release 复用，主构建从 `84s` 降到 `78s`，但新增恢复步骤 `13s`；本次主要变慢来自 pip cache 首次未命中，后端依赖安装从 `27s` 增至 `73s`，npm cache 也首次未命中并在 run 结束时写入缓存。

### 已同步到 CHANGELOG
- 已在 v1.2.1 记录发布预检脚本、快速发布流程文档、workflow pip cache、OpenCV runtime Release 资产复用、GitHub Actions 主版本升级和正式发布 workflow 参数修复。
