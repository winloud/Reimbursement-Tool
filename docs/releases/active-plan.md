# 当前开发计划

> 只记录当前目标、范围、验收条件和阻塞；完成结果、长期验证和技术决策分别写入 `CHANGELOG.md`、`docs/testing/`、`docs/decisions/`。正式发布前冻结本文件。

## 状态

- 版本号：v1.3.1
- 计划状态：进行中
- 预计版本类型：patch

## 目标

- [ ] 降低仓库治理、目录语义和大文件带来的非必要上下文成本。
- [ ] 保持业务行为和发布安全边界不变。

## 范围

- 本轮包含：产品文档真源、构建资产位置、测试结构，以及选定高上下文模块的渐进拆分。
- 本轮不包含：新增业务功能、正式发布、远端同步或 Linux 部署。

## 验收条件

- [x] 产品概览不再维护版本、测试或候选需求的手工索引。
- [x] 发布脚本不再修改稳定产品概览。
- [x] 仍参与构建的兼容二维码模型不再位于历史文档目录。
- [ ] 发布测试可按目录运行，遗留阶段测试按业务语义命名。
- [ ] 至少拆分一个前端和一个后端高上下文单体，行为保持不变。
- [ ] `Release` 与 `All` 验证档位通过。

## 阻塞

- 无。
