# 当前开发计划

> 只记录当前目标、范围、验收条件和阻塞；完成结果、长期验证和技术决策分别写入 `CHANGELOG.md`、`docs/testing/`、`docs/decisions/`。正式发布前冻结本文件。

## 状态

- 版本号：TBD
- 计划状态：开发与验收完成
- 预计版本类型：TBD

## 目标

- [x] 优化报销单录入页导航、汇总定位、渐进式信息展示和草稿保存体验。
- [x] 保持既有后端接口、数据结构和状态机不变，仅收紧前端交互边界。

## 范围

- 本轮包含：锚点吸顶偏移、宽屏汇总导航与右栏 sticky、预支和其他费用折叠、空白新增页延迟建单、按需取得报销单 ID、草稿进入已核对时的事由校验。
- 本轮不包含：后端 schema、公开 API、状态机规则、大范围组件拆分、既有空草稿清理、版本号调整、发布或部署。

## 验收条件

- [x] 空白新增页等待或退出均不创建记录；首次有效修改只创建一条草稿并在刷新后保留数据。
- [x] 空事由可保存普通草稿内容，只在草稿进入已核对时阻止流转；后续状态流转、回退和只读恢复正常。
- [x] 预支和其他费用按业务数据决定默认展开状态，折叠、刷新和只读切换不清空已有值。
- [x] 1836×1272、1280×720 和 390×844 下导航、sticky、锚点和横向溢出符合要求。
- [x] 自动化验证和生产构建通过，工作区仅包含本轮预期改动。

## 阻塞

- 无。
