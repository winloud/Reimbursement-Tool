# 当前开发计划

> 只记录当前目标、范围、验收条件和阻塞；完成结果、长期验证和技术决策分别写入 `CHANGELOG.md`、`docs/testing/`、`docs/decisions/`。正式发布前冻结本文件。

## 状态

- 版本号：v1.3.1
- 计划状态：进行中
- 预计版本类型：patch

## 目标

- [x] 降低仓库治理、目录语义和大文件带来的非必要上下文成本。
- [x] 保持业务行为和发布安全边界不变。
- [x] 调整总览看板的统计范围，并统一报销单状态标签的视觉语义。

## 范围

- 本轮包含：产品文档真源、构建资产位置、测试结构、选定高上下文模块的渐进拆分、总览看板统计范围调整，以及报销单状态标签样式统一。
- 本轮不包含：正式发布、远端同步或 Linux 部署。

## 验收条件

- [x] 产品概览不再维护版本、测试或候选需求的手工索引。
- [x] 发布脚本不再修改稳定产品概览。
- [x] 仍参与构建的兼容二维码模型不再位于历史文档目录。
- [x] 发布测试可按目录运行，遗留阶段测试按业务语义命名。
- [x] 至少拆分一个前端和一个后端高上下文单体，行为保持不变。
- [x] `Release` 与 `All` 验证档位通过。
- [x] 总报销金额和出差天数仅排除草稿；待报销金额仅统计已核对和已提交；月份范围趋势、费用类别排行和出差负荷热力图均仅排除草稿。
- [x] 草稿、已核对、已提交、已报销标签使用指定色值；报销单管理列表以固定同宽的前进／退回分体控件显示，中央状态文字居中；同状态多选时仅有一个次要流转则直接显示操作按钮，录入页操作按钮高度与管理页顶部操作一致，单张列表记录原地更新且不显示成功提示。
- [x] 已报销可直接回退至已提交；已提交和已报销均禁止修改报销单内容、发票和车票，回退到已核对后恢复编辑。

## 阻塞

- 无。
