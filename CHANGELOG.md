# 更新日志

本文件记录面向用户可见的版本变化，以及会影响交付、安装、发布或协作流程的重要变化。开发过程、实施细节和验证记录维护在 `docs/releases/active-plan.md`。

## Unreleased

## v1.4.2 - 2026-08-29

### Fixed

- 修复同一张多页发票因各页重复显示发票号而被误判为重复的问题：上传时按原始页序合并连续同号页面，并拆分不同发票号的明确连续分组；页面无法识别发票号或同号页面不连续时保留完整 PDF 作为待确认发票，并提醒用户必要时先拆分。
- 修复多页发票合并为一条记录后统计张数偏少的问题：电子发票按附件实际页数计入报销单、行程、费用项目、常规报销和 PDF 单据张数，一份四页发票计为四页；上传列表仍显示为一份发票。

## v1.4.1 - 2026-08-27

### Added

- 出差报销新增按“报销人 + 自然日”唯一的日期占用：已保存草稿会取得尚未被其他报销单占用的出差日期，重叠日期不再重复计入自动补贴天数；人工核定记录同样保留日期占用。

### Changed

- 发票金额不足提醒移至对应费用项目摘要，费用汇总不再重复显示该提醒；PDF 下载和状态变更门禁保持不变。
- 看板的金额、单数、费用分布、出差天数、月度趋势和出差负荷热力图全部纳入已保存草稿，草稿金额归入待报销；出差日期统一改用占用记录。完整数据包与数据库结构升级至 v7，旧数据和 v1–v6 数据包会按报销单顺序初始化日期占用。
- 数据维护页改为优先呈现程序更新，并重新分组版本、暂存包、备份恢复和诊断信息；ZIP 更新从选择、校验、原位确认到重启复用同一主操作，安装后的信息刷新不再让整页闪烁或打断焦点。
- “添加费用”菜单不再提供“行李费”和“未乘卧铺补助”，既有报销单中的历史费用仍保留显示与导出能力。
- “自定义费用”菜单项移除前置加号，并与燃油补助一样支持手动指定报销金额；留空时仍按已确认电子发票和纸质发票合计，指定金额超过票额时会提示补足后再提交或下载。

### Fixed

- 报销日期在首次提交前保持为空，标记“已提交”时写入当日日期，从已提交状态回退时同步清空。
- 修复仅填写出差事由的新草稿手动保存后被排到列表后续分页的问题；无报销日期且无行程的记录使用创建日期参与默认排序。
- 删除行程、其他费用项或常规报销项时会同步软删除其关联电子发票与凭据，并清空随父项保存的纸质票据，不再要求先逐个清理文件；同时修复删除行程后车船费发票变成无归属记录的问题。
- 修复从“添加费用”菜单选择固定费用类别后明细区没有自动展开的问题。
- 修复“新增报销单”下拉菜单宽度过窄、与触发按钮左右边界不一致的问题。
- 收窄桌面端展开态导航栏宽度，减少侧栏空白并保留导航文字可读性。
- 修复程序更新成功后更新暂存 ZIP 未清理的问题；数据维护的程序更新区现在支持逐包查看和手动清理更新暂存包。

## v1.4.0 - 2026-08-18

### Added

- 新增独立的常规报销单，创建时可选“无票报销”或“有票报销”且保存后不可切换；常规报销使用独立列表、筛选、四张汇总卡、数据包导入与当前筛选导出入口和编辑页，不进入原有出差列表与看板。
- 常规报销项目新增稳定编号、改名、重名、排序和逐项文件归属：无票模式可选上传 PDF 或图片凭据并按页数统计，有票模式按已确认发票自动汇总金额和张数。
- 新增常规报销 PDF 的独立模板分派、四行分页、字段映射和逐项附件合并逻辑；内置独立矢量模板，报销内容与备注左对齐并保留左右间隙，金额格在最高位前显示 `￥`。

### Changed

- 侧栏保持一级菜单，在“出差报销单”和“常规报销单”之间增加分隔线；顶部新增菜单同时提供出差、常规无票和常规有票入口。
- 常规报销列表移除报销人展示列，四个顶部操作按钮在桌面端保持同排，列表行可直接进入编辑；出差报销列表的编辑、预览、下载和删除／恢复操作统一改为直接图标。
- 常规报销列表顶部标题和操作按钮统一沿用出差列表的字号、尺寸与栅格；四张汇总卡统一采用总览看板的色条、标签、金额字号和辅助说明层级。
- 桌面端页面滚动时左侧主导航保持固定，导航内容过长时在侧栏内部滚动；移动端继续沿用上下布局。
- 常规报销编辑页对齐出差编辑页的顶部保存与状态操作、发票卡片样式和桌面双列项目布局；新增项目改为卡片末尾的占位入口并默认展开。
- 常规报销单的 PDF 文件名改用项目摘要，格式为“报销日期-报销人-项目摘要-有票/无票-金额”；单张下载与批量 ZIP 内文件名保持一致。
- 差旅与常规报销的电子发票、非发票附件和报销凭据上传入口合并到已上传文件列表末尾；空白卡片点击仅聚焦以支持 `Ctrl+V`，文件选择继续使用独立按钮，只读状态不显示上传入口。
- 修正常规报销编辑页手动保存后项目卡片意外折叠的问题；新增项目现在会先自动保存并立即开放凭据或发票上传，无需再次手动保存。
- 数据结构和完整数据 ZIP 升级至 v6，旧数据默认按出差报销解释，并继续支持 v1-v5 数据包导入；备份恢复、完整性检查、回收站和彻底删除同步覆盖常规项目及关联文件。
- 出差与常规填报页统一为同一套页面框架：页头标题统一为“出差报销单／常规报销单 #编号”，保存状态标签、只读与错误提示、章节卡片（含基本信息卡）、发票卡片列表、PDF 预览弹窗与费用汇总栏构图两页一致；行程卡圆角回归全站 8px 基线。
- 常规填报页交互补齐到出差页同等水准：项目卡的上移、下移和删除提升到标题行随时可用；汇总栏常驻显示“能否生成 PDF”的状态与原因（未确认发票或信息不完整），预览、下载按钮在受阻时置灰并弹窗解释；批量上传显示页面级进度条与文件名；手动保存在无修改时禁用。
- 出差与常规填报页的上传控件统一为同一形态：发票位与附件、凭据位都改为“图标 + 单行文案 + `Ctrl+V` 徽标 + 选择文件”，可上传类型白名单也合并为同一份。已上传附件与凭据改用与发票卡片同构的两列列表，并逐行对齐发票卡：第一行为“文件名 + 文件类型”，第二行为“页数 + 查看和删除按钮”，查看图标统一为眼睛（附件仍在新标签页打开）。出差页“非发票附件”章节改用统一章节头与章节卡，外框圆角回到全站 8px 基线。
- 出差行程卡与常规项目卡的排序方式统一：两处都提供左侧拖拽手柄和标题行的上移、下移按钮；拖拽限定在手柄上，不再与展开折叠、输入框抢交互。
- 出差填报页的 PDF 门槛改用与常规页相同的判定结构，汇总栏提示与受阻弹窗共用同一句说明；燃油补助发票不足时下载按钮由不可点改为置灰可点并弹窗解释原因。
- 出差行程卡的出发、到达日期改用与常规报销一致的日历输入，直接选择含年份的完整日期；「时」保留为手填项，出发与到达各压缩为一行，行程卡明显变矮。
- 行程日期现在按完整日期存库，跨年行程不再依赖按报销单日期猜年份：补贴天数、统计和 PDF 都以实际日期为准，铁路电子客票导入直接写入车票上的日期。升级时旧行程会按原推断规则一次性补齐年份，回收站中的报销单一并处理；个别日期本身矛盾的历史行程会保留空日期，在编辑页提示补填。
- 出差与常规填报页的成组卡片由两列卡片墙改为单列：行程卡与常规项目卡各占整行，宽屏下出差行程的出发、到达和交通工具排成一行，常规项目的备注并入字段行，出差基本信息的四个字段也压到同一行。已上传发票、附件和凭据的小卡改为按可用宽度自适应列数，一行可放更多张。
- 出差“其他费用”改为单张容器卡内逐行排列：只显示已录入内容、已有发票或本次手动添加的类别，不再固定占满 7 行空位；每行的报销、发票和张数三列对齐，行内可直接移除（有发票时需先删除发票），添加改为章节头的“添加费用”菜单，末项仍是自定义费用。PDF 仍按固定 7 行输出，不受影响。
- 差旅与常规报销填报页依据行程时间线原型统一为块卡与折叠行：差旅行程改为带补贴区间、衔接提示和行级操作的时间线，其他费用类别与常规报销项目改为浅灰摘要行拉开白色抽屉；抽屉内发票卡和上传槽回到浅灰层，常规报销项目在宽屏以单行摘要对齐，基本信息字段保留一致内边距，页面窄屏下保持可换行且无横向溢出。
- 细化填报页摘要层级：行程起止按钮组两侧增加更宽的分隔留白，其他费用摘要只显示报销金额和单据数；移除会随页头增加高度的区块导航条，保留错误状态触发的锚点回跳。
- 从列表进入报销单时，已加载的行程卡、其他费用项目和常规报销项目默认收起；手动添加、导入或生成的新项目默认展开，方便立即录入。

### Fixed

- 发票上传的文件选择框补齐 `application/pdf` 类型，部分系统下不再把 PDF 过滤掉。

- 报销单每次标记为“已提交”时，报销日期都会更新为当日；单张、批量以及从已报销退回已提交的状态操作保持一致，编辑页会立即显示新日期。
- “导出当前筛选”改为通过可重复访问的短时真实 URL 下载，IDM 接管后重新请求不再因缺少原 POST 请求体返回空内容；差旅与常规报销列表在勾选多张报销单后新增“导出已选”。
- 常规报销 PDF 模板改为按扫描原版完整宽度、顶部 `105.13 mm` 原尺寸校准；扫描校对不再裁掉四边或拉伸底图，运行时文字域、金额格和品牌元素同步回到原表毫米坐标。
- 修复人民币大写金额在部分跨万数值中多插入“零”的问题，例如 `1234573.89` 现在正确输出“壹佰贰拾叁万肆仟伍佰柒拾叁元捌角玖分”。
- 恢复回收站中的报销单时，不再一并恢复该报销单放入回收站之前已单独删除的发票。
- 常规报销编辑器在删除凭据、删除发票或确认发票后刷新前，会先保存当前草稿修改，避免自动保存等待期内的项目内容被旧数据覆盖。
- 差旅编辑路由不再误载常规报销记录；常规汇总加载失败时会显示明确提示，不再以 0 金额伪装成有效结果。
- v6 数据包导入会严格校验常规报销类型、模式、项目关联、发票类别、隐藏差旅字段和非草稿完整性，失败时回滚数据库并清理暂存文件。
- 报销 PDF 改为生成后通过真实下载链接交给浏览器处理：IDM 等下载工具可正常接管，未接管时由报销工具的独立浏览器配置选择保存路径，不再额外产生空白 `expense-report.pdf`；WebView2 模式同步开放系统保存对话框。
- 数据导出、备份和诊断包下载会拒绝空内容，并延迟释放临时下载地址，避免浏览器尚未读取完成就失效。
- 拖动行程卡或常规项目卡时卡片不再抖动：激活描边改用 outline，不再挤占内容宽度导致行内字段换行。
- 出差行程的日期输入不再被截断，宽屏下可完整显示带年份的日期。

## v1.3.2 - 2026-08-10

### Added

- 每张报销单新增一个独立的“非发票附件”区域，位于其他费用之后，支持上传、查看和删除 PDF 或图片；附件不计入发票数量，完整数据导入导出和“有附件”筛选均可识别。
- 左侧导航栏新增可记忆的紧凑模式；收起后仅保留新增、导航和展开图标，鼠标悬停或键盘聚焦时显示用途提示。
- 报销单新增页改为首次有效修改后再创建草稿；完全空白的报销单不再自动保存，退出时也不再显示空草稿处理提示。发票上传和车票解析等需要报销单 ID 的操作会在真正执行时按需创建草稿。
- 预支信息改为紧凑折叠区，已有日期或金额时自动展开；其他费用按项目折叠，仅有电子发票、纸质发票、手填金额、备注或自定义类别时默认展开，折叠和只读切换均保留已有值。

### Changed

- 报销单录入页的“基本信息”和“非发票附件”标题移至内容卡片外，与“行程列表”保持同一区块层级；非发票附件上传区同步采用既有发票上传区的聚焦、拖入和接收反馈。
- 报销 PDF 的附件顺序固定为先行程发票、再其他费用发票，项目内按上传顺序排列，最后追加报销单级非发票附件；图片按原始尺寸生成 PDF 页面，不主动缩放或压缩。
- 前端正式支持范围调整为 CSS 逻辑宽度不低于 1280px 的桌面窗口；更窄窗口仅尽力展示，不再新增专用布局或纳入常规视觉回归与发布阻塞条件。
- 其他费用中的“过路费”统一更名为“通行费”，内部类别键 `toll` 保持不变，兼容已有报销数据。
- 出差事由只在草稿进入已核对时校验；空事由仍可保存其他内容，已核对后的后续流转与回退不重复校验。

### Fixed

- 修复高系统缩放下双列行程卡片标题和发票状态意外换行，以及状态夹在行程信息与操作之间的问题；发票状态固定归入第二行摘要并降低视觉强调，无发票时不再重复显示 0 张与 ¥0.00。卡片头直接保留“起／止”和返程操作，复制、交换与删除收进更多菜单，长标题与摘要改为单行省略并可悬停查看全文，宽屏汇总栏同步收紧以给行程区留出更多空间。
- 修复 4K 显示器在 Windows 250% 缩放下报销单管理列表的日期、表头和操作区换行问题；日期与短字段保持单行，行内仅保留预览、下载，编辑／查看与删除收进更多菜单，低于表格语义最小宽度时改为横向滚动而非撑高数据行。
- 回收站列表中的状态改为只读展示，行内仅保留恢复操作，彻底删除收进更多菜单并继续要求二次确认；收紧操作列后，在 4K 显示器 250% 缩放下不再裁切右侧操作。
- 导入数据包的冲突明细新增稳定列宽、粘性表头和横向滚动；来源 UID 单行省略并可复制，原因限制为两行，手机宽度下改用紧凑卡片展示。
- 修复报销单录入页点击锚点后标题被吸顶导航遮挡的问题；超宽屏隐藏“汇总”导航入口，较窄桌面和手机继续保留。
- 费用汇总的宽屏 sticky 定位移至右栏包装层，长表单滚动时汇总卡可稳定停留在吸顶导航下方。

## v1.3.1 - 2026-07-30

### Changed

- 报销单管理列表的批量“修改状态”现可直接选择任一不同目标状态，并逐条反馈更新或跳过原因；草稿直接进入任一后续状态时，会统一校验出差事由和燃油补助发票金额。已核对、已提交和已报销之间的状态调整不重复校验只读资料。
- 已核对状态现与已提交、已报销一样不可修改报销单内容、发票和车票；回退草稿后恢复编辑。燃油补助发票不足时仍可预览 PDF，单张下载和批量下载的既有校验保持不变。
- 勾选报销单后，页面右上角的固定按钮区切换为批量操作态，筛选区和表格均保持原位；连续勾选相邻行时，复选框位置和键盘焦点保持稳定。
- 总览看板的总报销金额和出差天数现累计全部非草稿报销单；待报销金额仅累计已核对和已提交报销单。月份范围趋势、费用类别排行和出差负荷热力图均改为仅排除草稿。
- 草稿、已核对、已提交和已报销状态标签更新为统一的语义色；报销单管理列表以同宽的前进／退回分体控件显示，可直接切换相邻状态；同状态多选时唯一的次要流转直接显示为按钮，录入页操作按钮高度与管理页顶部操作一致。单张记录切换状态时原地更新，不再显示成功提示或重新加载列表。
- 已报销报销单可直接回退至已提交；从已提交开始，报销单内容、发票与车票均不可修改，回退到已核对后才可继续编辑。

## v1.3.0 - 2026-07-28

### Added

- 普通 PDF / 图片发票和铁路电子客票现会检查全库有效电子发票；文件内容或发票号任一重复时均禁止入库，并提示重复记录来源。查重覆盖草稿、已核对、已提交和已报销记录，回收站、已删除发票和纸质发票不参与检查。
- Windows 本机访问报销工具时，“发票信息确认”窗口可使用系统默认 PDF 程序打开发票原始 PDF；远程服务器访问和图片原件继续在浏览器新标签页中打开。
- 所有行程和费用类别现在均可按需登记纸质发票的合计金额和张数；默认仅显示“添加纸质发票”按钮，录入后收起为紧凑摘要。纸质发票无需上传附件，会直接纳入报销金额、PDF 表单、统计和相关发票校验。
- 报销单途中补贴新增“自动计算”和“人工核定补贴总额”两种方式；费用汇总以紧凑开关即时切换方式且不弹窗，自动状态不显示编辑入口，人工状态仅在金额旁显示编辑图标并通过金额对话框修改；人工核定适用于短时外出合并、工作餐已安排等例外情形，不使用补贴天数，PDF 仅填写最终补贴总额。
- 报销单新增“已核对”状态，形成“草稿、已核对、已提交、已报销”的完整流程；管理列表支持勾选多张报销单后批量修改状态，并逐条提示不符合流转条件的记录。

### Changed

- 报销单管理列表在“报销总金额”后新增“发票总数”列，合并显示未删除电子发票和已登记纸质发票的数量。
- 原“已打印”状态统一显示为“已提交”；既有报销单和内部 `printed` 数据无需转换，新增状态的数据域版本升级至 v4，并继续兼容旧数据包导入。
- PDF 预览、单张下载和批量下载调整为纯输出操作，不再自动修改报销单状态或报销日期；状态改由用户在列表中点击状态徽标直接流转，同状态批量选择可一键执行下一步，混合选择会显示各目标状态的可更新与跳过数量。

### Fixed

- 统一普通发票批量上传的异常结果提示：全部重复、部分重复和普通上传失败现在均在“发票上传结果”弹窗中分类展示；部分成功时可先查看重复或失败明细，再选择继续确认已上传发票或稍后处理。
- 全站描边输入框的聚焦态取消缺口边框阴影，改用 2px 蓝色边框和白底浮动标签，避免阴影从标签背后透出、影响文字辨识。
- 修复人工核定金额编辑框中“人工核定补贴总额”浮动标签上沿被弹窗内容区裁切的问题；桌面窗口和手机宽度下均可完整显示标签文字。
- 修复 Windows 本地发布包的数据兼容清单仍写入旧版数据结构范围的问题；测试包和正式包现在会与应用当前数据结构版本保持一致。
- 修复已报销报销单在录入页无法预览或下载 PDF 的问题；草稿、已核对、已提交、已报销现在均可使用 PDF 操作，原有发票校验规则保持不变。
- PDF“其他费用-项目”列的最大字号限制为 9.7pt，避免短项目名称以过大字号输出。
- 修复桌面端全屏与窗口化状态在宽度接近 2560 CSS px 时，内容区最大宽度跨断点突变、导致行程卡片明显缩窄的问题；内容区现在会连续放宽，并保持宽屏居中和 1920px 上限。
- 修复 Chrome/Edge 独立窗口最小化时把 Windows 的隐藏哨兵坐标写入 `window-state.json` 的问题；升级后会忽略历史无效坐标，窗口将使用默认位置启动并恢复正常记忆。

## v1.2.6 - 2026-07-25

### Added

- 报销单行程支持批量导入铁路电子客票 PDF：可在导入前修正识别字段，并将原始车票直接关联为对应行程的车船费发票。
- 铁路客票中文按 PDF 字体编码直接恢复，不依赖 OCR 或英文站名；多张票在站点连续且日期为同日或次日时默认合并，明显返程或路线回环自动拆分。
- 车票导入预览支持删除单张车票（例如重复项），仅导入需要保留的车票，无需退回重新上传整批。

### Changed

- 报销单录入的费用汇总卡改为车船费、途中补贴、其他费用和汇总四个区块；其他费用不再显示金额为 `¥0.00` 的分类明细。
- 燃油补助手填报销金额在删除或调低发票后会保留；发票金额不足时会显示缺口提示，仍可保存和预览，但单张/批量下载及标记为已打印会要求先补充足额发票。已打印报销单若因该类修改变为不足，会自动退回草稿。
- 报销单编辑页的行程月、日、时字段改为三个对齐列（最小可完整显示两位数字），地点独占第二行以保持视觉平衡；已上传发票改为更紧凑的双列卡片，金额、类型和确认状态统一基线显示，保留查看、删除操作；长发票号会省略显示。
- 高铁车票导入区与发票上传区统一：支持拖入、Ctrl+V 粘贴和选择 PDF，并提供焦点、拖入及接收文件的高亮反馈；剪贴板 PDF 会补充后端可识别的文件名。

### Fixed

- 兼容铁路客票将中文站名主体和末尾“站”使用不同字体、不同文字运行绘制的版式，避免站名文字可复制但导入预览仍要求人工补充。
- 修复多张车票受文件名或选择顺序影响而漏识别中转的问题；首次预览会按日期和站点连接关系自动整理无歧义路线链，分叉或循环仍保守交由人工确认。
- 批量导入车票时，单个损坏或非铁路电子客票 PDF 只跳过并在预览中提示，不再整批中断，其余车票照常识别导入。
- 修复行程卡的月、日、时、地点输入框在窄卡片中被压缩，日期 `23` 等两位数现在可完整显示。
- 移除所有数字输入框的浏览器原生上下微调箭头，金额仍可直接输入两位小数并沿用原有校验。

## v1.2.5 - 2026-07-13

### Changed

- 正式发布流程调整为可重复准备、单命令续跑：默认运行发布总控只准备版本文件并预检，`-Publish` 从已有状态续跑到 GitHub Release 校验完成，发布成功后不再自动产生第二次文档提交和 push。
- GitHub Actions 和 GitHub Release 成为公开发布状态真源；正式远端 tag 永不移动，发布失败保留 release commit 和 tag 并从原 tag 续跑，源码需要变化时改发新的 patch 版本。
- 冻结版本计划统一使用“内容已冻结”，并要求在创建正式 tag 前完成冻结；重要人工安装、升级或迁移验证仍可按需作为普通文档提交记录。
- 区分发票上传入口和已上传发票列表：上传入口改为浅蓝操作带，选中后使用与拖入相同的上浮、阴影和扫光反馈；发票列表增加标题、数量提示和确认状态侧边色。

## v1.2.4 - 2026-07-13

### Added

- 报销单发票上传区支持 Ctrl+V 粘贴剪贴板中的 PDF 或图片，并复用现有批量上传和发票确认流程。

### Changed

- 前端 UI 第一轮工作台化打磨：统一主题、卡片、表格、输入框和状态色；看板费用分布改为类别排行，报销单列表固定批量操作条，报销单录入页新增锚点导航、行程路线标题和 PDF 阻塞提示。
- 改进发票上传区视觉反馈：移除偏重的装饰图标；点击或键盘选中后改为浅蓝底和实线边框，拖入时继续突出目标费用区并播放落位反馈。

### Fixed

- 修复总览看板在真实数据加载后 390px 窄屏下出现轻微横向滚动的问题。

## v1.2.3 - 2026-06-27

### Added

- 发布总控脚本新增失败回滚提示，当 GitHub Actions workflow 失败时，提供交互式选项：删除远端 tag 并重置本地分支、只删除远端 tag 保留本地 commit 供调查，或保持现状手动处理。

### Changed

- 出差补贴「起/止」模型重新设计：补贴默认按“第一段出发日 → 最后一段到达日”整段计算并覆盖中间所有自然日，单次往返无需任何标记，也不再依赖出发地、到达地名称判断；只有一次出差中途回家或转往其他地点、需要把中间某段排除在补贴之外时，才手动用「止 / 起」切分区间。「生成返程」会自动把返程标记为本次出差的收尾，「复制行程 / 添加行程」会把新行程追加到末尾，并在上一段已收尾时自动接续为下一次出差。此行为调整会影响补贴天数与报销金额。
- 各页面内容宽度统一对齐，并适配笔记本、27 英寸、32 英寸等不同宽度显示器；报销单录入页「基本信息」卡片字段区左右留白对齐。
- 个性化设置页改为统一的全宽卡片布局，与数据维护页风格保持一致：默认报销信息、编辑体验、发票二维码识别、PDF 导出、PDF 填充字体各自独立成卡。
- 行程卡片移除折叠功能，行程内容保持常显。

### Fixed

- 报销单管理列表对非法行程日期等脏数据容错，单条脏数据不再导致整页加载失败。
- 修复历史报销单中单次往返被算成两个单天区间、漏算中间天数的补贴错误；程序启动时会一次性清洗历史「起/止」标记并按新规则重算补贴天数。
- 修复添加行程后上一段残留「止」标记截断补贴区间、导致补贴天数显示为 0 的问题。

## v1.2.2 - 2026-06-25

### Added

- 新增 `scripts/release_publish.ps1` 发布总控脚本，可自动准备版本文件、冻结发布计划、运行预检、创建 release commit/tag，并在 `-Publish` 时推送 tag、等待 GitHub Actions、校验资产、采集耗时并写回验证记录。
- 新增 `scripts/validate_release_asset.ps1`，可校验本地发布 ZIP、只检查 GitHub Release 资产元数据，或按需下载远端主 ZIP 做深校验；下载资产时会自动重试。
- 新增 `scripts/collect_release_metrics.ps1`，可采集 GitHub Actions run 的总耗时和步骤耗时，并输出 JSON 或 Markdown，支持与基线 run 对比。
- 发布总控脚本新增显式重发既有 tag 和忽略未跟踪本地草稿文件的参数，便于修正同版本 Release 或在干净的已跟踪文件状态下继续发布。

### Changed

- 发布流程文档改为优先使用总控脚本，手工步骤保留为故障排查或脚本不可用时的 fallback。
- GitHub Release workflow 在上传资产前对 runner 本地构建出的主 ZIP 做内容校验；总控脚本发布后默认只做 Release 资产元数据校验，避免因网络慢反复下载主 ZIP。
- 正式发布流程明确为先合并并推送 `main`，再从 `main` 创建 release commit 和 tag；发布总控脚本在 `-Publish` 时默认阻止从非 `main` 分支发布。
- 发布总控脚本等待 GitHub Actions 时只匹配本次 tag push 之后的新 run，避免重发既有 tag 时误抓历史 run。
- 发布总控脚本和 GitHub Actions 本地 ZIP 校验改为用 PowerShell 返回状态而不是 `LASTEXITCODE` 判断 PowerShell 子脚本，避免空值误判失败。

## v1.2.1 - 2026-06-25

### Added

- 新增 `scripts/prepare_release.ps1` 发布预检脚本，用于在不重复本地正式打包的情况下校验版本号、CHANGELOG、README、冻结计划、release notes、测试和 diff。
- 新增 `docs/release-process.md`，记录以 GitHub tag workflow 产物为准的快速发布路径。

### Changed

- GitHub 正式发布和 preview artifact workflow 启用 Python pip cache，减少后端与打包依赖重复下载时间。
- GitHub 正式发布 workflow 优先复用已有 OpenCV runtime Release 资产，仅在缺少匹配 OpenCV 包版本资产时重建。
- GitHub Actions workflow 升级 checkout、setup-python、setup-node 和 upload-artifact action 主版本，避免旧 Node.js action runtime 弃用提示。
- GitHub 正式发布 workflow 改用 PowerShell 显式参数表调用打包脚本，避免正式发布日期被误判为预览流水号。

## v1.2.0 - 2026-06-25

### Added

- 新增程序内“数据维护”能力：可创建完整备份 ZIP、下载最近备份、上传备份 ZIP 预览并执行恢复，恢复前会自动生成恢复前备份。
- 新增诊断信息和诊断包导出，包含版本号、数据目录、QR 引擎、浏览器/WebView2 状态、日志路径、配置摘要、运行配置摘要、环境信息、日志尾部和可读摘要，便于排查问题。
- 新增数据库完整性检查，可在“数据维护”中检查 SQLite 物理完整性、外键、业务一致性和发票附件状态。
- 新增 ZIP 本地升级辅助脚本 `upgrade_zip_release.ps1`，用于从旧版 `报销管理` 目录复制运行态数据到新版目录，并在复制前创建升级备份。
- 桌面窗口会记住用户调整后的大小和位置，下次启动时自动恢复。
- 新增便携式安装根目录：根目录 `报销管理.exe` 作为 launcher，真实程序按版本保存在 `versions/<version>/`。
- 新增程序内更新：在“数据维护”中选择新版发布 ZIP，预览后安装，安装前自动创建完整备份，重启后生效。
- 新增程序内版本切换：可在“数据维护”中切换到已安装版本，切换前自动创建完整备份，重启后生效。
- 新增数据结构兼容性门禁：程序内更新和已安装版本切换会检查当前数据库结构版本是否在目标版本支持范围内，缺少兼容性信息或不兼容时禁止自动安装/切换。
- 数据维护页新增备份文件和已安装版本管理，可删除选中备份/版本，并一键清理旧备份或旧版本；清理备份会保留最近备份，清理版本会保留当前版本。
- 图片格式发票新增二维码识别能力；未识别到二维码时仍可手动确认金额。
- PDF 发票新增逐页识别能力；一个多页 PDF 中识别到多张发票时，会创建多条发票记录并逐张确认。
- 新增 GitHub Actions 手动 preview artifact 构建入口，可零输入在云端生成 preview ZIP 供测试下载，不创建 GitHub Release。

### Changed

- 发布 ZIP 会携带 `upgrade_zip_release.ps1`，继续保持不包含 `data/`、`uploads/`、`logs/`、`browser-profile/` 等运行态目录。
- “数据维护”从个性化设置中拆出为独立页面，升级、备份恢复和诊断导出不再挤在个性化设置里。
- 报销单管理列表调整为优先展示“出差开始日期 / 出差结束日期 / 报销日期”，便于按行程时间核对。
- 报销单管理列表默认按出差开始日期倒序排列；无行程日期的报销单排在后面。
- 数据维护页为“备份恢复”补充独立标题和说明，并在程序更新安装完成后提供“重启程序”按钮。
- 侧边栏导航将“数据维护”移到末尾，降低日常录入路径中的干扰。
- 优化数据维护页结构：将备份恢复、程序更新、诊断检查拆成独立任务区，并让更新、恢复、诊断错误就近显示在对应区域。
- 已安装版本再次选择对应 ZIP 时，会提示使用版本切换而不是重复安装已有版本目录。
- 发布 ZIP 的 `报销管理/` 目录改为稳定安装根目录，运行数据继续保存在该根目录，程序版本保存在 `versions/`。
- 批量删除、批量彻底删除、导入覆盖和报销单状态回退前会自动创建安全快照；快照创建失败时会中止原操作。
- 新增 ZIP 本地安装、程序内更新和备份指南，并同步到根目录 README。
- 预览打包命名规则调整为 `报销管理-preview-yyyymmdd-NNN.zip`；如果 active-plan 已定义目标版本 `vX.Y.Z`，则使用 `报销管理-vX.Y.Z-preview-yyyymmdd-NNN.zip`，避免预览包占用或混淆正式 `vX.Y.Z` 版本号。

### Fixed

- 修复报销单编辑页在自动保存等待期间上传发票会重新加载服务端旧数据，导致未保存的出差事由和行程内容被清空的问题；现在上传前会先保存当前修改。
- 修复云端 preview artifact 下载后不能直接用于程序内更新的问题；现在 artifact ZIP 顶层就是便携更新包内容。
- 桌面版选择备份 ZIP 进行恢复时，文件选择窗口默认打开应用备份目录；非桌面环境仍使用浏览器默认文件选择。
- 修复草稿报销单 PDF 预览、单张下载和批量下载未按实际生成日期更新“报销日期”的问题；现在会先更新报销日期，再生成预览图片、PDF 内容和文件名，并在预览后同步刷新页面日期，已打印或已报销记录不会被自动改动报销日期。
- 修复桌面版程序内重启只打开新窗口、不关闭旧窗口的问题；Chrome/Edge app-mode 会先关闭旧应用窗口，再启动新版本。

## v1.1.1 - 2026-06-20

### Added

- 新增源码开发服务重启入口：`restart-dev.cmd` 调用 `scripts/restart-dev.ps1`，可统一重启后端 `127.0.0.1:8000` 和前端 `127.0.0.1:5174`。
- 新增 AI 协作规则入口 `AGENTS.md`，并用 `CLAUDE.md` 指向同一套规则。
- 新增开发文档入口 `docs/README.md` 和当前开发计划 `docs/releases/active-plan.md`。
- 燃油补助新增可手动填写的报销金额；为空时仍按已确认发票合计计算。
- 个性化设置新增自动保存延时，可在 3-60 秒之间调整报销单编辑页的自动保存等待时间。
- 新增 GitHub tag 发布工作流：推送 `v*` 标签后自动构建主 ZIP 和可选 OpenCV runtime ZIP，并从本文件对应版本段落生成 GitHub Release notes。
- GitHub Release 资产使用 ASCII 文件名 `reimbursement-tool-vX.Y.Z-yyyymmdd.zip` 上传，避免 GitHub 规范化非 ASCII 资产名时剥掉中文；本地发布 ZIP 仍保持 `报销管理-vX.Y.Z-yyyymmdd.zip`。

### Changed

- 发布 ZIP 命名规则改为 `报销管理-vX.Y.Z-yyyymmdd.zip`。
- 发布脚本不再清空 `release/` 目录，也不覆盖已有主 ZIP 或 OpenCV runtime ZIP；如同名文件已存在，脚本会报错并要求手动删除。
- 发布脚本默认版本更新为 `1.1.1`。
- 发布脚本支持显式传入 `-ReleaseDate yyyymmdd`，供 GitHub Actions 按中国时区固定发布日期。
- 根目录 `README.md` 保持最终用户发布说明定位，移除源码开发服务重启说明。
- 报销单编辑页自动保存改为延时保存，并新增手动保存；PDF 预览、下载、状态流转和页面导航前会先尝试保存未提交修改。
- 总览看板出差负荷热力图色阶改为从绿色过渡到赭黄色，便于区分连续出差负荷。
- 个性化设置页改为更紧凑的设置面板布局，并将字体授权风险和内置字体目录提示归入 PDF 填充字体卡片。
- 行程日期校验仅对跨年到达的单段行程保留 7 天限制；同年跨月长行程可以正常保存。

### Fixed

- 修复 Linux 服务器部署时 PDF 字体列表为空，导致个性化设置无法保存默认报销信息的问题；服务器可通过本地字体目录提供宋体、黑体、微软雅黑、楷体和仿宋。
- 修复 Linux 服务器部署时报销单 PDF 其他费用项目名未按模板规则使用楷体的问题。
- 修复自动保存错误提示占用页面空间、干扰录入焦点的问题。
- 修复燃油补助按发票合计全额计入报销总额的问题；现在可按报销标准填写较低金额，且不能超过已确认发票合计。
- 修复同年跨月长行程被误判为“到达日期不能早于出发日期”的问题。

### Documentation

- 主开发计划瘦身为产品总览、当前状态、能力基线和关键索引。
- 将 V1.1.0 详细开发记录冻结到 `docs/releases/v1.1.0-plan.md`。
- 新增发票二维码识别路线决策记录 `docs/decisions/0001-invoice-qr-engine.md`。
- 将 240 个发票样本的二维码路线对照测试记录移动到 `docs/testing/`。
- 新增 Linux 服务器部署文档 `docs/deployment/linux-server.md`，记录 Python 3.13、systemd、Nginx 和服务器本地字体部署要求。
- 新增 `CHANGELOG.md`，并明确 `active-plan` 记录过程、`CHANGELOG` 记录面向用户和交付流程的结果。
- 冻结本轮发布记录到 `docs/releases/v1.1.1-plan.md`，并重建下一轮 `docs/releases/active-plan.md`。

## v1.1.0 - 2026-06-09

### Added

- 新增发票二维码识别引擎设置，可在 `zxing-cpp` 和 OpenCV WeChatQRCode 兼容模式之间切换。
- 新增 OpenCV WeChatQRCode 可选 runtime 包，本地放到 EXE 同级后可由程序安装到运行目录。

### Changed

- 默认发票二维码识别路线改为 `zxing-cpp`。
- 主发布 ZIP 不再包含 OpenCV、NumPy 和 WeChatQRCode 模型，减小默认发布包体积。
- OpenCV runtime ZIP 文件名使用 OpenCV 包版本号，例如 `opencv-wechat-runtime-opencv-4.10.0.84-win_amd64.zip`。
- 发布包清理运行态目录，主 ZIP 不包含 `data/`、`uploads/`、`logs/`、`browser-profile/`。

### Fixed

- OpenCV 兼容模式运行时缺失或损坏时，发票解析记录诊断并回退到 `zxing-cpp`，避免解析流程中断。
- 处理 OpenCV WeChatQRCode 模型在中文路径下加载失败的问题。

### Verification

- 使用 `test example/` 中 240 个 PDF 发票样本完成两条二维码识别路线对照测试，zxing 与 OpenCV 的 payload 和最终解析结果均 `240/240` 一致。

## v1.0.0 - 2026-06-09

### Added

- 发布 Windows 本地桌面版报销管理工具。
- 支持报销单管理、行程录入、发票上传、金额确认、PDF 预览/下载、状态流转、筛选批量操作、回收站、导入导出、看板和个性化设置。
- 支持 PDF 发票和图片发票上传、预览、金额解析与手动确认。
- 支持报销单 PDF 预览、下载，以及下载成功后自动标记为已打印。
- 支持报销数据 ZIP 导入导出，导入执行前自动备份数据库和受影响附件。
- 支持 PyInstaller onedir 本地发布包，最终用户不需要安装 Python、Node.js 或 npm。
